# The URL the site will be built for
# base_url = "https://www.zymologia.fi/"
base_url = "https://evgenii-barannik.github.io/Alzy-site-test"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

title = "Alzymologist Oy"
description = "Laboratory for microbrewers"
generate_feed = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
hyde_sticky = true
hyde_reverse = false
hyde_theme = ""
hyde_links = [
    {url = "$BASE_URL/info/contacts#starthere", name = "Contacts"},
    {url = "$BASE_URL/info/services#starthere", name = "Services"},
    {url = "$BASE_URL/info/methods#starthere", name = "Lab analysis"},
    {url = "$BASE_URL/info/pricing#starthere", name = "Pricing"},
    {url = "$BASE_URL/info/about#starthere", name = "About"},
    {url = "$BASE_URL/info/gdpr#starthere", name = "Privacy policy"},
    {url = "$BASE_URL/atom.xml", name = "Atom feed"},
]
